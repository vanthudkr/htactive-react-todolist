import React from "react";

const SignOut = props => {
  return (
    <li>
      <button className="btn">
        <a href="SignIn">Sign out</a>
      </button>
    </li>
  );
};

export default SignOut;
